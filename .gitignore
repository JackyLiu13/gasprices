# WiFi credentials live here — never commit
firmware/gasprices/config.h

# host test binaries
tests/test_verdict
tests/test_ui

# golden images for the preview's pixel diff — regenerated, never reviewed
tests/out/

# Derived from the CSVs by backend/db.py in a millisecond. Regenerated, never
# reviewed — same reasoning as the golden images above.
backend/analytics.db

__pycache__/
*.pyc
.DS_Store

# SAM build artifacts — regenerated by `sam build`
.aws-sam/
